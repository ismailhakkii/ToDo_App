name: todo_app
description: A ToDo uygulaması
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
    
  shared_preferences: ^2.0.17
  provider: ^6.0.5
  awesome_notifications: ^0.10.0
  flutter_local_notifications: ^17.2.3

  image_picker: ^1.0.6
  path_provider: ^2.0.15
  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/placeholder.jpg
